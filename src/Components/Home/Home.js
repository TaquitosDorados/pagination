import React from 'react';
import List from '../newCharacter/List';

function Home(props) {
    return (
        <div className= "container">
            <List/>
        </div>
    );
}

export default Home;